[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback — deep links like /destination/paris resolve to the app.
# Function routes (/api/weather) are matched before this catch-all.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
